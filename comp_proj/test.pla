include "pla_lib/std/io.pla"a;

function main : $() -> s32 {
	std::init_con_io();

	variable ch : u16;

	while ch != 10'u16 {
		ch = std::read_con_ch();
		std::write_con_ch(ch);
	}

	return %[s32]ch;
}